"id": "fixed-scripts",
"scripts": {
  "dev": "tsx server.ts",
  "build": "vite build && tsc",
  "preview": "vite preview",
  "clean": "rm -rf dist",
  "lint": "tsc --noEmit",
  "start": "node dist/server.js"
}
